<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Email OTP Verification</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: 'Segoe UI', sans-serif;
            background: radial-gradient(circle at top left, #001f1f, #002b2b, #003b3b);
            color: #00ffcc;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .otp-container {
            max-width: 400px;
            width: 90%;
            background: rgba(0, 255, 204, 0.05);
            backdrop-filter: blur(15px);
            border-radius: 20px;
            padding: 30px;
            border: 1px solid rgba(0, 255, 204, 0.2);
            box-shadow: 0 0 20px rgba(0, 255, 204, 0.15);
        }

            .otp-container h2 {
                text-align: center;
                margin-bottom: 25px;
                color: #00ffcc;
            }

            .otp-container input[type="text"] {
                width: 100%;
                padding: 12px 15px;
                margin-bottom: 20px;
                border: none;
                border-radius: 12px;
                background: rgba(255, 255, 255, 0.07);
                color: #00ffcc;
                font-size: 16px;
                box-shadow: inset 0 0 5px #00ffcc;
                outline: none;
            }

                .otp-container input[type="text"]::placeholder {
                    color: #88ffee;
                }

            .otp-container button {
                width: 100%;
                padding: 12px;
                background: #00ffcc;
                color: #001f1f;
                border: none;
                border-radius: 12px;
                font-weight: bold;
                font-size: 16px;
                cursor: pointer;
                transition: 0.3s ease;
            }

                .otp-container button:hover {
                    background: #00e6b8;
                    box-shadow: 0 0 10px #00ffcc;
                }

        .otp-error {
            text-align: center;
            color: #ff4d4d;
            margin-top: 10px;
            font-weight: bold;
        }

        .otp-success {
            text-align: center;
            color: #00ffcc;
            margin-top: 10px;
            font-weight: bold;
        }
    </style>
</head>
<body>
    <div class="otp-container">
        <h2>Email Verification</h2>
        <form method="post">
            <input type="text" name="otp" placeholder="Enter OTP" required />
            <button type="submit">Verify</button>
        </form>

        @* Razor conditional messages *@
        @if (ViewBag.Error != null)
        {
            <div class="otp-error">@ViewBag.Error</div>
        }
        @if (ViewBag.Success != null)
        {
            <div class="otp-success">@ViewBag.Success</div>
        }
    </div>
</body>
</html>
